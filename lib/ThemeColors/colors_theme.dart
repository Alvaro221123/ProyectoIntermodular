import 'dart:ui';

// Colores principales

class AppColors {
  static const Color primary = Color(0xFF2F80ED);
  static const Color secondary = Color(0xFF27AE60);
  static const Color other = Color(0xFFF2C94C);

  // Backgrounds

  static const Color background = Color(0xFF2F80ED);
  //static const Color backgroundComponent = Color(0xFF291462);
  //static const Color backgroundComponentSelected = Color(0xC03E4EA0);
}